# Release 26.9.16

เวอร์ชัน: `26.9.16+2`

## สรุป

รวมงานปรับปรุง UI หลายหน้า แก้ปัญหารูปโปรไฟล์ไม่แสดง จัดลำดับรายชื่อบุคลากรใหม่
และเปิดใช้ GitHub Pages สำหรับ deploy เว็บอัตโนมัติ

## รูปโปรไฟล์ (แก้ปัญหารูปไม่แสดง)

- **ต้นเหตุ**: `lh3.googleusercontent.com` จำกัดจำนวนคำขอต่อเครื่องผู้ใช้
  พอหน้าจอโหลดรูปพร้อมกันหลายรูป Google จะตอบ **HTTP 429** แล้วรูปหายทั้งหน้า
- เพิ่ม `lib/utils/profile_image.dart` เป็นตัวกลางของทั้งแอป
  - `resolveDisplayImageUrl()` แปลงลิงก์ Google Drive ได้ทุกรูปแบบ
    (`/file/d/`, `open?id=`, `uc?export=view`, `thumbnail?id=`)
  - วิดเจ็ต `ProfileAvatar` ใช้พร็อกซี wsrv.nl เป็นหลัก → ถอยไป lh3 → ถอยไปตัวอักษรย่อ
  - ใช้ `webHtmlElementStrategy.prefer` เลี่ยงปัญหา CORS บนเว็บ
- เปลี่ยนมาใช้ตัวกลางนี้ทุกหน้า: แถบเมนูซ้าย, ตารางจัดการผู้ใช้, หน้าบุคลากร,
  โปรไฟล์มือถือ, ฟอร์มใบลามือถือ, ใบรับรองแพทย์ในประวัติการลา
- แถบเมนูซ้ายดึงรูปล่าสุดจาก Supabase เอง ไม่ต้องล็อกอินใหม่
  (`findTeacher()` หาแถวจาก `id_user` → `username` → `fullName`)

## การเรียงลำดับรายชื่อ

- เพิ่ม `lib/utils/teacher_sort.dart` ใช้ร่วมกันทุกหน้า
- ลำดับ: กลุ่มสาระ (ฝ่ายบริหาร → ภาษาไทย → คณิตศาสตร์ → ...)
  → ตำแหน่งบริหาร → วิทยฐานะ → ชื่อ
- ใช้กับหน้าบุคลากรและรายงานสรุปการลา (รวมถึงไฟล์ PDF/Excel ที่ export)

## ปรับ UI

- **จัดการระบบ**: แถบแท็บขยับชิดขอบบน ตารางยืดเต็มความสูงจอ, เอาปุ่ม `Export to Supabase` ออก
- **แดชบอร์ด**: หน้าไม่เลื่อนแล้วเมื่อเนื้อหาพอดีจอ
- **ประวัติการลา**: ตัดหัวข้อ/ปุ่มย้อนกลับออก, ยุบตัวกรองเหลือแถวเดียว
  (ปีงบประมาณ · กลุ่มสาระ · วิทยฐานะ · ตำแหน่ง · สิทธิ์ · ค้นหา · ปุ่มค้นหา · รีเฟรช),
  เพิ่มตัวกรองวิทยฐานะและสิทธิ์, เอาแถบแจ้งเตือนการเลือกด้านบนออกเหลือปุ่มลอยปุ่มเดียว
- **ปฏิทิน**: เปลี่ยนชื่อหัวข้อเป็น "ปฏิทิน โรงเรียนรมบุรีพิทยาคม",
  กดการ์ดรายชื่อคนลาเพื่อเปิดฟอร์มใบลาแบบ popup ได้

## Deploy

- เพิ่ม `.github/workflows/deploy-pages.yml` — push ขึ้น `main` แล้ว build Flutter web
  และ deploy ขึ้น GitHub Pages อัตโนมัติ
- เว็บ: https://omkub.github.io/koragoch/

## หมายเหตุ

- Tag: `26.9.16`
