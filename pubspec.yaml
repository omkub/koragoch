name: school_leave_app
description: A new Flutter project for School Leave System.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^3.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  image_picker: ^1.1.0
  http: ^1.6.0
  shared_preferences: ^2.2.0
  google_fonts: ^6.2.0
  csv: ^8.0.0
  fl_chart: ^1.2.0
  intl: ^0.20.2
  url_launcher: ^6.3.2
  file_picker: ^8.1.2
  rxdart: ^0.28.0
  firebase_auth: ^5.7.0
  flutter_localizations:
    sdk: flutter
  thai_buddhist_date_pickers: ^0.1.6
  thai_buddhist_date: ^0.2.7
  table_calendar: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - image/Logo.jpg
