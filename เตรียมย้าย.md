# เตรียมย้ายข้อมูล Firebase ไป Supabase

เอกสารนี้ใช้รวบรวมข้อมูลเพื่อวางแผนย้ายจาก Firebase Firestore ไป Supabase เท่านั้น ยังไม่มีการย้ายหรือลบข้อมูล และไม่มีการแก้ไขโค้ดแอป

## สถานะและแหล่งข้อมูล

- รายการด้านล่างมาจากชื่อ collection ที่พบในผลค้นหาโค้ด และรายชื่อตาราง Supabase จากภาพที่ส่งมา
- รายชื่อ collection นี้ยังไม่ใช่การยืนยันว่าครบทุก collection ในฐานข้อมูลจริง
- โค้ดระบุ Firestore database ID เป็น `school` ควรยืนยันว่าเป็นฐานข้อมูลที่ใช้งานจริง
- ต้องตรวจ Firebase Console เพิ่ม เพื่อดู collection ที่โค้ดไม่ได้เรียกใช้, subcollection และจำนวนเอกสาร

## Collection ที่พบในโค้ด

| Firebase collection | ตาราง Supabase ที่เสนอ | ความหมายเบื้องต้น |
|---|---|---|
| `Teachers` | `Teachers` | ครูและข้อมูลผู้ใช้งาน |
| `UserRoles` | `UserRoles` | ความสัมพันธ์ผู้ใช้กับบทบาท |
| `Permissions` | `Permissions` | สิทธิ์การใช้งาน |
| `MobilePermissions` | `MobilePermissions` | สิทธิ์การใช้งานบนมือถือ |
| `Leaves` | `Leaves` | รายการลา |
| `LeaveTypes` | `LeaveTypes` | ประเภทการลา |
| `FiscalRounds` | `FiscalRounds` | รอบปีงบประมาณ |
| `SpecialHolidays` | `SpecialHolidays` | วันหยุดพิเศษ |
| `SpecialWorkingDays` | `SpecialWorkingDays` | วันทำงานพิเศษ |
| `Subjects` | `Subjects` | วิชา |
| `LoginLogs` | `LoginLogs` | ประวัติการเข้าสู่ระบบ |
| `Settings` | `Settings` | การตั้งค่าระบบ |

พบ document ภายใน `Settings` อย่างน้อย 3 รายการ: `app_config`, `line_messaging`, `dropdowns`

## การเทียบกับตาราง Supabase ในภาพ

- พบ `Subjects` ในโค้ด แต่ไม่เห็นตาราง `subjects` ในภาพ
- ตาราง `academics`, `adminroles`, `appconfig`, `departments`, `positions` และ `roles` ปรากฏในภาพ แต่ยังระบุ collection ต้นทางไม่ได้
- `appconfig` อาจเกี่ยวข้องกับ `Settings/app_config` แต่ต้องเทียบฟิลด์ก่อน

## จุดที่ต้องตรวจเพิ่มก่อนออกแบบ schema

1. อ่าน `lib/services/migration_service.dart` และตรวจชื่อตัวแปร `collectionName`, `masterCollection`, `_permissionCollectionName`
2. ตรวจ collection และ subcollection จริงใน Firebase Console รวมทั้งจำนวน document
3. เก็บ document ID เดิมและชื่อฟิลด์ ชนิดข้อมูล ค่าว่าง ฟิลด์ที่ขาด
4. ตรวจ map, array, document reference และ timestamp
5. ตรวจความสัมพันธ์ระหว่างครู ผู้ใช้ บทบาท สิทธิ์ ประเภทลา และรอบงบประมาณ
6. ตรวจ Firebase Authentication และ Storage หากแอปใช้งาน
7. สำรองข้อมูลต้นทางก่อนเริ่มทดลองนำเข้า

## ตารางข้อมูลที่ควรจัดทำระหว่างสำรวจ

| Collection | จำนวนเอกสาร | ฟิลด์หลัก | Document ID | ความสัมพันธ์ | หมายเหตุ |
|---|---:|---|---|---|---|
| Teachers | รอตรวจ | รอตรวจ | รอตรวจ | ผู้ใช้/บทบาท | ตรวจ UID และข้อมูลส่วนบุคคล |
| UserRoles | รอตรวจ | รอตรวจ | รอตรวจ | Teachers, Roles | ยืนยันรูปแบบบทบาท |
| Permissions | รอตรวจ | รอตรวจ | รอตรวจ | Roles | ตรวจรูปแบบสิทธิ์ |
| MobilePermissions | รอตรวจ | รอตรวจ | รอตรวจ | Roles/ผู้ใช้ | ตรวจข้อมูลซ้ำ |
| Leaves | รอตรวจ | รอตรวจ | รอตรวจ | Teachers, LeaveTypes, FiscalRounds | ตรวจวันที่และสถานะ |
| LeaveTypes | รอตรวจ | รอตรวจ | รอตรวจ | Leaves | ตรวจโควตาและเงื่อนไข |
| FiscalRounds | รอตรวจ | รอตรวจ | รอตรวจ | Leaves | ตรวจปีและช่วงวันที่ |
| SpecialHolidays | รอตรวจ | รอตรวจ | รอตรวจ | ปฏิทิน | ตรวจเขตเวลา |
| SpecialWorkingDays | รอตรวจ | รอตรวจ | รอตรวจ | ปฏิทิน | ตรวจเขตเวลา |
| Subjects | รอตรวจ | รอตรวจ | รอตรวจ | ข้อมูลการลา | ยังไม่เห็นตารางปลายทางในภาพ |
| LoginLogs | รอตรวจ | รอตรวจ | รอตรวจ | ผู้ใช้ | ตรวจข้อมูลส่วนบุคคล |
| Settings | รอตรวจ | app_config, line_messaging, dropdowns | รอตรวจ | ระบบ | แยก document กับ table ให้ชัดเจน |

## แนวทาง mapping เบื้องต้น

- คง document ID เดิมไว้ในคอลัมน์ `firebase_id` จนกว่าจะตรวจสอบการใช้ ID ครบ
- อย่าเพิ่ม foreign key จนกว่าจะยืนยันว่าค่าต้นทางมีอยู่จริงและชนิดข้อมูลตรงกัน
- map และ array อาจใช้ `jsonb` หรือแยกเป็นตารางลูกตามการใช้งานจริง
- แยกวันที่รายการลาออกจาก `created_at` และ `updated_at` และตรวจเขตเวลา
- ออกแบบ Supabase Auth และ RLS หลังเข้าใจการตรวจสิทธิ์เดิม
- ค่าลับของ `line_messaging` ควรเก็บเป็น secret/environment variable

## ลำดับการย้ายที่เสนอ

1. สำรวจ collection, subcollection, ฟิลด์ และจำนวนเอกสารจริง
2. สำรองข้อมูล Firebase และจัดทำ mapping
3. ออกแบบ schema, index, foreign key และ RLS บนฐานทดสอบ
4. นำเข้าข้อมูลชุดทดลองตามความสัมพันธ์
5. เปรียบเทียบจำนวนรายการ, ID, ความสัมพันธ์ และวันเวลา
6. ทดสอบสิทธิ์และการทำงานของแอปใน Supabase
7. จัดทำแผนสลับระบบและย้อนกลับก่อนย้ายจริง

## Checklist

- [x] รวบรวมชื่อ collection จากโค้ด 12 รายการ
- [x] รวบรวม document ของ `Settings` 3 รายการ
- [x] บันทึกจุดต่างกับตาราง Supabase ในภาพ
- [ ] ตรวจ `migration_service.dart`
- [ ] ตรวจ collection และ subcollection ใน Firebase จริง
- [ ] เก็บจำนวนเอกสารและโครงสร้างฟิลด์
- [ ] ยืนยัน mapping และความสัมพันธ์
- [ ] เตรียม SQL schema หลังตรวจข้อมูลจริง
- [ ] ทดลองนำเข้าและตรวจความถูกต้อง

ยังไม่ควรสร้าง SQL schema ฉบับใช้งานจริงจนกว่าจะได้โครงสร้างฟิลด์และความสัมพันธ์จากฐานข้อมูลจริง

## วิธีจัดการข้อมูลที่แนะนำ: สำรวจทั้งหมด แล้วลงรายละเอียดทีละ table

ไม่ควรย้ายหรือออกแบบทีละ table แบบแยกขาดจากกันตั้งแต่ต้น เพราะจะมองไม่เห็น foreign key, document ID และความสัมพันธ์ระหว่างข้อมูล วิธีที่เหมาะสมคือทำ 2 ระยะดังนี้

### ระยะที่ 1: สำรวจภาพรวมทั้งหมดก่อน

รวบรวมชื่อ collection/table ทั้งหมด จำนวน document ฟิลด์หลัก document ID และความสัมพันธ์แบบย่อให้ครบก่อน โดยยังไม่ต้องลงรายละเอียดทุกฟิลด์และยังไม่ต้องนำเข้าข้อมูลจริง ผลลัพธ์ของระยะนี้คือแผนผังข้อมูลและลำดับ dependency เช่น `Teachers` ต้องพร้อมก่อนข้อมูลที่อ้างถึงครู และ `LeaveTypes` กับ `FiscalRounds` ต้องพร้อมก่อน `Leaves`

### ระยะที่ 2: วิเคราะห์ทีละ table

หลังมีภาพรวมแล้ว ให้เลือก table ทีละรายการมาวิเคราะห์ละเอียดในไฟล์นี้ โดยใช้แบบฟอร์มเดียวกันทุก table:

1. ชื่อ collection ต้นทางและ table ปลายทาง
2. จำนวน document และตัวอย่าง document ที่ปกปิดข้อมูลส่วนตัวแล้ว
3. รายการฟิลด์ ชนิดข้อมูล ค่าว่าง ค่าเริ่มต้น และการแปลงชื่อ
4. document ID เดิมและฟิลด์ที่ใช้เป็นรหัสอ้างอิง
5. ความสัมพันธ์กับ table อื่น
6. กฎตรวจสอบข้อมูลและรายการข้อมูลผิดรูปแบบ
7. แนวทาง schema, index และ RLS ใน Supabase
8. วิธีตรวจผลหลังนำเข้าข้อมูล

### ลำดับ table ที่ควรเริ่ม

1. `Teachers` → `teachers` เพราะหลายข้อมูลอาจอ้างถึงครูหรือผู้ใช้
2. `LeaveTypes` → `leavetypes`
3. `FiscalRounds` → `fiscalrounds`
4. `Subjects` → `subjects`
5. `Permissions` และ `MobilePermissions`
6. `UserRoles` และบทบาทที่เกี่ยวข้อง
7. `SpecialHolidays` และ `SpecialWorkingDays`
8. `Leaves` → `leaves` หลังยืนยัน foreign key ของตารางก่อนหน้า
9. `LoginLogs` → `loginlogs`
10. `Settings` → `settings` เป็นกลุ่มสุดท้าย เพราะต้องตัดสินใจก่อนว่าจะเก็บ document ย่อยเป็น JSONB หรือตารางแยก

ลำดับนี้เป็นเพียงลำดับวิเคราะห์เบื้องต้น ต้องปรับตาม dependency ที่พบจริงจากข้อมูล Firebase

### แบบฟอร์มวิเคราะห์ table รายตัว

คัดลอกแบบฟอร์มนี้ต่อท้ายเมื่อเริ่มวิเคราะห์แต่ละ table:

```markdown
### [ชื่อ table]

- Firebase collection:
- Supabase table:
- จำนวน document:
- Document ID:
- ฟิลด์อ้างอิง:
- ความสัมพันธ์:
- ฟิลด์และชนิดข้อมูล:
- กฎแปลงข้อมูล:
- ปัญหาข้อมูลที่พบ:
- Schema/Index/RLS ที่เสนอ:
- วิธีตรวจหลังนำเข้า:
- สถานะ: ยังไม่เริ่ม / กำลังตรวจ / ตรวจแล้ว / ทดลองนำเข้าแล้ว
```

### กติกาการทำงาน

- สำรวจภาพรวมทั้งหมดก่อน แต่ลงรายละเอียดทีละ table
- ยังไม่สร้าง foreign key หรือรัน SQL จริงจนกว่าจะยืนยันรหัสและชนิดข้อมูล
- ยังไม่ลบหรือแก้ข้อมูลต้นทางระหว่างสำรวจ
- เก็บข้อมูลตัวอย่างโดยปกปิดชื่อ เบอร์โทร อีเมล token และข้อมูลส่วนบุคคล
- เมื่อวิเคราะห์ table หนึ่งเสร็จ ให้บันทึกจำนวนต้นทาง จำนวนปลายทาง และรายการที่นำเข้าไม่สำเร็จ

สรุป: ใช้ไฟล์นี้เก็บภาพรวมทั้งหมดก่อน แล้วทำรายละเอียดทีละ table วิธีนี้วางแผนได้ถูกต้องและตรวจสอบย้อนกลับได้ง่ายกว่าการนำเข้าทีละ table โดยไม่มีภาพรวม

## กติกาการตั้งชื่อ table (ยืนยันตามแผนล่าสุด)

- ชื่อ Firebase collection และ Supabase table จะใช้ชื่อเดียวกันทั้งหมด เช่น `Teachers` → `Teachers` และ `Leaves` → `Leaves`
- ฟิลด์อาจใช้ชื่อเดิม หรือปรับชื่อ/ชนิดข้อมูลเมื่อจำเป็น โดยต้องบันทึก mapping ไว้ในแบบฟอร์มของ table นั้น
- ต้องตัดสินใจเรื่องตัวพิมพ์เล็ก–ใหญ่ให้ชัดเจนก่อนสร้าง SQL เพราะ PostgreSQL จะพับชื่อที่ไม่ใส่เครื่องหมายคำพูดเป็นตัวพิมพ์เล็ก
- เพื่อให้เรียกใช้จากแอปได้ตรงชื่อ หากต้องการรักษา CamelCase เช่น `Teachers` ต้องสร้างและอ้างอิงด้วยเครื่องหมายคำพูด เช่น `"Teachers"`; หากเลือกใช้ชื่อไม่ใส่เครื่องหมายคำพูด ชื่อตารางจริงจะกลายเป็นตัวพิมพ์เล็ก
- ในเอกสารนี้ให้ถือชื่อในคอลัมน์ Supabase เป็นชื่อเดียวกับ Firebase เป็นหลัก จนกว่าจะมีการตัดสินใจเรื่องตัวพิมพ์เล็ก–ใหญ่
- ชื่อ document ภายใน `Settings` เช่น `app_config`, `line_messaging` และ `dropdowns` เป็นข้อมูลภายใน table/โครงสร้างของ `Settings` ไม่ใช่ชื่อ table ใหม่โดยอัตโนมัติ

## ตารางอ้างอิงเดิมจาก Firebase (ก่อนจัดทำ SQL)

ตารางด้านล่างเก็บไว้เป็นข้อมูลต้นทางเดิม ส่วนโครงสร้าง Supabase ให้ยึด SQL ฉบับทบทวนท้ายเอกสาร แต่ละฝั่งเป็นรายชื่อฟิลด์แยกกัน ไม่ใช่การจับคู่ตามแถว

- หนึ่งแท็บต่อหนึ่ง table ใช้ชื่อแท็บเดิมทั้ง 18 รายการ
- มีเพียง 2 คอลัมน์: Firebase | Supabase โดยแต่ละแถวแสดงชื่อฟิลด์ที่เทียบกัน
- ไม่ใส่จำนวนรายการ สถานะ ความสัมพันธ์ หรือหมายเหตุเป็นแถวในชีต
- ชื่อฟิลด์ Firebase อ้างอิง ฐานข้อมูล.txt ซึ่งสรุปจากโค้ด ยังไม่ใช่การตรวจฐานข้อมูลจริง
- ฝั่ง Supabase ใช้ รอตรวจ จนกว่าจะมี schema จริง ห้ามเดาชื่อฟิลด์หรือถือว่าตรงกับ Firebase โดยอัตโนมัติ
- ถ้าแหล่งข้อมูลยังไม่ระบุชื่อฟิลด์ ใช้ รอตรวจ ทั้งสองช่อง
- Settings ใช้ document/field เช่น app_config/secretKey เพื่อแยกฟิลด์ของแต่ละ document; ไม่ใช่ชื่อฟิลด์ที่มีเครื่องหมาย / ในฐานข้อมูล
- Permissions รองรับชื่อเมนูภาษาไทยเดิมด้วย แต่เอกสารไม่ระบุชื่อ จึงแสดงเฉพาะ key 0–8 และ updatedAt ที่มีหลักฐาน
- ฐานข้อมูล.txt กล่าวถึง Academics, AdminRoles, Departments, Positions และ Roles แต่ไม่ระบุชื่อฟิลด์ และยังไม่ยืนยัน mapping กับแท็บตัวพิมพ์เล็กเดิม
- ตารางรวมเดิมเก็บเป็นแท็บซ่อนสำหรับอ้างอิง
- ปลายทาง: https://docs.google.com/spreadsheets/d/1pzgnPfxT0jjThW2EK0nLb43t1JoaVEzeZ_sYnmSlDHo/edit

### Teachers

| Firebase | Supabase |
|---|---|
| username | รอตรวจ |
| password | รอตรวจ |
| fullName | รอตรวจ |
| email | รอตรวจ |
| role | รอตรวจ |
| permission | รอตรวจ |
| position | รอตรวจ |
| department | รอตรวจ |
| profileImage | URL รูปโปรไฟล์ |
| firebase_uid | รอตรวจ |
| originalPassword | รอตรวจ |
| forgotPasswordStatus | รอตรวจ |
| tempResetCode | รอตรวจ |
| resetAllowedUntil | รอตรวจ |
| tempPassword | รอตรวจ |
| lastSyncAt | รอตรวจ |
| passwordMigratedAt | รอตรวจ |
| created_at | รอตรวจ |
| updated_at | รอตรวจ |

### UserRoles

| Firebase | Supabase |
|---|---|
| role | รอตรวจ |
| permission | รอตรวจ |
| fullName | รอตรวจ |
| teacherDocId | รอตรวจ |
| lastSyncAt | รอตรวจ |

### Permissions

| Firebase | Supabase |
|---|---|
| 0 | รอตรวจ |
| 1 | รอตรวจ |
| 2 | รอตรวจ |
| 3 | รอตรวจ |
| 4 | รอตรวจ |
| 5 | รอตรวจ |
| 6 | รอตรวจ |
| 7 | รอตรวจ |
| 8 | รอตรวจ |
| updatedAt | รอตรวจ |

### MobilePermissions

| Firebase | Supabase |
|---|---|
| 0 | รอตรวจ |
| 1 | รอตรวจ |
| 2 | รอตรวจ |
| 3 | รอตรวจ |
| 4 | รอตรวจ |
| 5 | รอตรวจ |
| 6 | รอตรวจ |
| 7 | รอตรวจ |
| 8 | รอตรวจ |
| updatedAt | รอตรวจ |

### Leaves

| Firebase | Supabase |
|---|---|
| uid | รอตรวจ |
| requestId | รอตรวจ |
| timestamp | รอตรวจ |
| status | รอตรวจ |
| lastUpdatedAt | รอตรวจ |
| fullName | รอตรวจ |
| userId | รอตรวจ |
| role | รอตรวจ |
| position | รอตรวจ |
| department | รอตรวจ |
| leaveDate | รอตรวจ |
| leaveType | รอตรวจ |
| reason | รอตรวจ |
| startDate | รอตรวจ |
| endDate | รอตรวจ |
| days | รอตรวจ |
| totalDays | รอตรวจ |
| year | รอตรวจ |
| receiveNumber | รอตรวจ |
| medicalCertificate | URL ใบรับรองแพทย์ |

### LeaveTypes

| Firebase | Supabase |
|---|---|
| รอตรวจ | รอตรวจ |

### FiscalRounds

| Firebase | Supabase |
|---|---|
| year | รอตรวจ |
| round | รอตรวจ |
| startDate | รอตรวจ |
| endDate | รอตรวจ |
| isActive | รอตรวจ |
| createdAt | รอตรวจ |

### SpecialHolidays

| Firebase | Supabase |
|---|---|
| date | รอตรวจ |
| dateValue | รอตรวจ |
| title | รอตรวจ |
| note | รอตรวจ |
| source | รอตรวจ |
| createdAt | รอตรวจ |
| updatedAt | รอตรวจ |

### SpecialWorkingDays

| Firebase | Supabase |
|---|---|
| รอตรวจ | รอตรวจ |

### Subjects

| Firebase | Supabase |
|---|---|
| รอตรวจ | รอตรวจ |

### LoginLogs

| Firebase | Supabase |
|---|---|
| username | รอตรวจ |
| fullName | รอตรวจ |
| role | รอตรวจ |
| timestamp | รอตรวจ |
| platform | รอตรวจ |
| userAgent | รอตรวจ |

### Settings

| Firebase | Supabase |
|---|---|
| app_config/secretKey | รอตรวจ |
| app_config/appsScriptUrl | รอตรวจ |
| app_config/driveProfileFolderId | รอตรวจ |
| app_config/driveLeaveFolderId | รอตรวจ |
| line_messaging/groupId | รอตรวจ |
| line_messaging/webhookUrl | รอตรวจ |
| line_messaging/template | รอตรวจ |
| line_messaging/updatedAt | รอตรวจ |
| dropdowns/positions | รอตรวจ |
| dropdowns/ranks | รอตรวจ |
| dropdowns/departments | รอตรวจ |
| dropdowns/roles | รอตรวจ |
| dropdowns/adminPositions | รอตรวจ |
| dropdowns/leaveTypes | รอตรวจ |

### academics

| Firebase | Supabase |
|---|---|
| รอตรวจ | รอตรวจ |

### adminroles

| Firebase | Supabase |
|---|---|
| รอตรวจ | รอตรวจ |

### appconfig

| Firebase | Supabase |
|---|---|
| รอตรวจ | รอตรวจ |

### departments

| Firebase | Supabase |
|---|---|
| รอตรวจ | รอตรวจ |

### positions

| Firebase | Supabase |
|---|---|
| รอตรวจ | รอตรวจ |

### roles

| Firebase | Supabase |
|---|---|
| รอตรวจ | รอตรวจ |

## SQL สร้างตาราง Supabase จากชีต — ฉบับทบทวน 2026-09-07

ส่วนนี้เป็นร่าง SQL ล่าสุดสำหรับสร้างตารางใหม่ อ้างอิงชื่อจากชีตและปรับตามการทบทวนที่ผู้ใช้อนุญาต ยังไม่ได้รันกับฐานข้อมูลจริงหรือแก้โค้ดแอป

สิ่งที่เพิ่มจากชีตเดิมในการทบทวนครั้งนี้: PK อัตโนมัติของ SpecialWorkingDays, LoginLogs, Settings และคอลัมน์ FiscalRounds.year ส่วนชื่อคอลัมน์อื่นยังคงตามร่างเดิม ไม่มีการอัปเดต Google Sheets ในครั้งนี้

แหล่งข้อมูล: [Google Sheets ข้อมูลวันลา](https://docs.google.com/spreadsheets/d/1pzgnPfxT0jjThW2EK0nLb43t1JoaVEzeZ_sYnmSlDHo/edit) อ่านคอลัมน์ Supabase ของแต่ละแท็บแยกจาก Firebase ไม่ถือว่าแถวซ้าย–ขวาเป็น mapping โดยตรง

### กติกาและสมมติฐานของร่าง SQL

- รักษาชื่อตารางและคอลัมน์ตามชีตด้วย double quotes รวมชื่อที่มีช่องว่าง
- PK ที่เป็นรหัสของตารางเองใช้ bigint GENERATED ALWAYS AS IDENTITY เริ่ม 1 เพิ่มครั้งละ 1; FK ใช้ bigint และไม่รันเลขเอง
- ทุกตารางที่สร้างมี PK ของตัวเอง รวม id_SpecialWorkingDays, id_LoginLogs และ id_Settings ที่เพิ่มในการทบทวน
- LoginLogs.id_user เป็นรหัสผู้ใช้ที่รอกำหนด FK ไม่ใช่รหัสประวัติการเข้าใช้; id_LoginLogs เป็น PK อัตโนมัติแยกต่างหาก ผู้ใช้คนเดียวจึงมีประวัติได้หลายรายการ
- appconfig.ID_AppConfig เป็น PK ของตารางเอง จัดให้อยู่คอลัมน์แรกของ SQL ตามกติกาของแผน โดยไม่เปลี่ยนชื่อ
- Leaves.status ใช้ text ตามที่ยืนยัน เช่น ส่งใบแล้ว, รออนุมัติ, ไม่อนุมัติ; ไม่กำหนด CHECK รายการสถานะเพราะยังไม่ได้ยืนยันว่าครบทุกค่า
- MobilePermissions.status ใช้ bit(1) NOT NULL รับ B'0' หรือ B'1' ตามที่ยืนยัน; เอา CHECK ที่ซ้ำกับชนิด bit(1) ออกและห้าม NULL เพื่อให้มีสองสถานะเท่านั้น ไม่มี default จึงต้องส่งค่าสถานะทุกครั้งที่ INSERT
- bit(1) ของ PostgreSQL เป็น bit string; ตัวอย่าง SQL ใช้ B'0'/B'1' และต้องตรวจรูปแบบส่งค่าของ client ก่อนใช้งาน ไม่ถือว่าเทียบเท่าเลข integer 0/1 โดยอัตโนมัติ
- ยังไม่กำหนดว่า MobilePermissions แต่ละแถวเป็นสิทธิ์เมนูใด จึงยังใช้แทนสิทธิ์ 0–8 ไม่ครบจนกว่าจะยืนยันความสัมพันธ์นี้
- ชนิดข้อมูลที่เหลือเป็นข้อเสนอจากชื่อฟิลด์ ไม่ใช่ schema ที่ตรวจจากฐานข้อมูลจริง: วันที่ใช้ date, เวลาใช้ timestamptz, totalDays ใช้ numeric(10,2), isActive ใช้ boolean, round/year/lastNumber ใช้ integer, ข้อความอื่นใช้ text
- dateValue เก็บ text ชั่วคราว เพราะยังไม่ทราบว่าเป็นวันที่ ข้อความ หรือเลข epoch; adminPositions/departments/positions ใน Settings เก็บ text ชั่วคราว ต้องยืนยันว่าควรเป็น jsonb/array หรือตารางอ้างอิง
- บังคับ NOT NULL เฉพาะ PK และ MobilePermissions.status; คอลัมน์อื่นยังอนุญาต NULL รวม FK จนกว่าจะยืนยันข้อมูลบังคับ ไม่มี default หรือ UNIQUE เพิ่มเติม
- FK ทั้ง 3 จุดอ้าง PK ชนิด bigint และใช้ NO ACTION ตามค่าเริ่มต้น ไม่ลบข้อมูลลูกอัตโนมัติ
- FiscalRounds.id_year เป็นรหัสอัตโนมัติ ส่วน year เป็น integer เก็บปีงบประมาณจริง เช่น 2569; ต้องใช้ระบบปี พ.ศ./ค.ศ. ให้สอดคล้องตอนนำเข้าและแสดงผล
- ไม่มีค่า default เวลาและไม่มี trigger อัปเดต updatedAt; ผู้นำเข้าหรือแอปต้องส่งค่าเอง
- เปิด RLS ทุกตาราง แต่ยังไม่สร้าง policy จึงยังไม่พร้อมให้แอปทั่วไปอ่าน/เขียน ต้องออกแบบสิทธิ์ก่อนเปิดใช้งาน โดยเฉพาะ Settings ที่มี secretKey และ channelToken
- SQL ใช้สำหรับตารางที่ยังไม่มีเท่านั้น ไม่ใช่คำสั่งปรับโครงสร้างตารางเดิม หากมีชื่อซ้ำ CREATE TABLE จะล้มเหลวและ transaction ไม่สำเร็จ ไม่มี DROP หรือคำสั่งลบข้อมูล

### จุดที่ยังต้องยืนยัน

| ตาราง/รายการ | สถานะ |
|---|---|
| Teachers, UserRoles, Permissions | ยังไม่มีคอลัมน์ Supabase ที่ยืนยัน จึงไม่สร้างตารางว่างหรือเดาฟิลด์ |
| SpecialWorkingDays, LoginLogs, Settings | เพิ่ม PK อัตโนมัติใน SQL แล้ว ชื่อใหม่คือ id_SpecialWorkingDays, id_LoginLogs, id_Settings; ต้องใช้ชื่อเหล่านี้ในแผนและแอปเมื่อพัฒนาต่อ |
| Leaves.id_user, LoginLogs.id_user | คงคอลัมน์ bigint ไว้ ยังไม่ใส่ FK เพราะตารางผู้ใช้ปลายทางไม่ยืนยัน |
| MobilePermissions.id_role | อ้าง roles.ID_Roles ตามความหมายที่ยืนยัน ชื่อ FK/PK ต่างกันได้; ถ้าต้องการชื่อเดียวกันต้องปรับแผน |
| FiscalRounds | เพิ่ม year แยกจาก id_year แล้ว; ยังต้องยืนยันระบบปี กฎข้อมูลบังคับ และเงื่อนไขปี/รอบซ้ำ |
| appconfig.Key AppTitle | ร่างถือเป็นชื่อคอลัมน์เดียวตามข้อความในชีต ต้องยืนยันว่าไม่ได้หมายถึง Key ที่มีค่า AppTitle |
| Subjects | ไม่พบแท็บในชีตล่าสุด จึงไม่สร้างตาราง |

### คำสั่ง SQL (14 ตาราง — ยังไม่ใช่ schema พร้อมใช้งานจริง)

```sql
BEGIN;

CREATE TABLE public."MobilePermissions" (
    "id_MobilePermissions" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "id_role" bigint,
    "status" bit(1) NOT NULL,
    "updatedAt" timestamptz
);

CREATE TABLE public."Leaves" (
    "id_leaves" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "id_user" bigint,
    "timestamp" timestamptz,
    "status" text,
    "lastUpdatedAt" timestamptz,
    "leaveDate" date,
    "id_leaveType" bigint,
    "reason" text,
    "startDate" date,
    "endDate" date,
    "totalDays" numeric(10,2),
    "id_year" bigint,
    "receiveNumber" text,
    "medicalCertificate" text
);

CREATE TABLE public."LeaveTypes" (
    "id_leaveType" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "leaveName" text,
    "createdAt" timestamptz,
    "updatedAt" timestamptz
);

CREATE TABLE public."FiscalRounds" (
    "id_year" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "year" integer,
    "round" integer,
    "startDate" date,
    "endDate" date,
    "isActive" boolean,
    "createdAt" timestamptz
);

CREATE TABLE public."SpecialHolidays" (
    "id_holiday" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "date" date,
    "dateValue" text,
    "title" text,
    "note" text,
    "source" text,
    "createdAt" timestamptz,
    "updatedAt" timestamptz
);

-- เพิ่ม PK ของตารางเองในการทบทวน
CREATE TABLE public."SpecialWorkingDays" (
    "id_SpecialWorkingDays" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "createdAt" timestamptz,
    "date" date,
    "dateValue" text,
    "note" text,
    "title" text,
    "updatedAt" timestamptz
);

-- PK ของประวัติแยกจาก id_user ซึ่งอาจซ้ำได้หลายรายการ
CREATE TABLE public."LoginLogs" (
    "id_LoginLogs" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "id_user" bigint,
    "timestamp" timestamptz,
    "platform" text,
    "userAgent" text
);

-- เพิ่ม PK ของตารางเองในการทบทวน; รูปแบบเก็บกลุ่มการตั้งค่ายังต้องยืนยัน
CREATE TABLE public."Settings" (
    "id_Settings" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "appsScriptUrl" text,
    "driveLeaveFolderId" text,
    "driveProfileFolderId" text,
    "secretKey" text,
    "adminPositions" text,
    "departments" text,
    "positions" text,
    "channelToken" text,
    "groupId" text,
    "template" text,
    "updatedAt" timestamptz,
    "webhookUrl" text,
    "lastNumber" integer,
    "year" integer
);

CREATE TABLE public."academics" (
    "ID_Academics" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "createdAt" timestamptz,
    "updatedAt" timestamptz,
    "AcademicsName" text
);

CREATE TABLE public."adminroles" (
    "ID_AdminRoles" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "createdAt" timestamptz,
    "updatedAt" timestamptz,
    "AdminRolesName" text
);

CREATE TABLE public."appconfig" (
    "ID_AppConfig" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "Description" text,
    "Key AppTitle" text,
    "Value" text
);

CREATE TABLE public."departments" (
    "ID_Departments" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "createdAt" timestamptz,
    "updatedAt" timestamptz,
    "DepartmentsName" text
);

CREATE TABLE public."positions" (
    "ID_Positions" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "createdAt" timestamptz,
    "positionName" text,
    "updatedAt" timestamptz
);

CREATE TABLE public."roles" (
    "ID_Roles" bigint GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    "createdAt" timestamptz,
    "updatedAt" timestamptz,
    "Access rights" text,
    "License" text
);

ALTER TABLE public."Leaves"
    ADD CONSTRAINT "Leaves_id_leaveType_fkey"
    FOREIGN KEY ("id_leaveType") REFERENCES public."LeaveTypes" ("id_leaveType");
CREATE INDEX "Leaves_id_leaveType_idx" ON public."Leaves" ("id_leaveType");

ALTER TABLE public."Leaves"
    ADD CONSTRAINT "Leaves_id_year_fkey"
    FOREIGN KEY ("id_year") REFERENCES public."FiscalRounds" ("id_year");
CREATE INDEX "Leaves_id_year_idx" ON public."Leaves" ("id_year");

ALTER TABLE public."MobilePermissions"
    ADD CONSTRAINT "MobilePermissions_id_role_fkey"
    FOREIGN KEY ("id_role") REFERENCES public."roles" ("ID_Roles");
CREATE INDEX "MobilePermissions_id_role_idx" ON public."MobilePermissions" ("id_role");

-- Leaves.id_user และ LoginLogs.id_user: รอยืนยันตารางผู้ใช้และ PK ปลายทางก่อนเพิ่ม FOREIGN KEY
-- updatedAt / lastUpdatedAt ไม่เปลี่ยนอัตโนมัติ: ต้องกำหนดจากแอปหรือเพิ่ม trigger ภายหลัง

ALTER TABLE public."MobilePermissions" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."Leaves" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."LeaveTypes" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."FiscalRounds" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."SpecialHolidays" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."SpecialWorkingDays" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."LoginLogs" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."Settings" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."academics" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."adminroles" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."appconfig" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."departments" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."positions" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."roles" ENABLE ROW LEVEL SECURITY;

COMMIT;
```

### การตรวจสอบร่าง

- ร่างมี 14 ตาราง อ้างอิงรายชื่อจากชีตที่อ่านเมื่อสร้างร่าง และเพิ่ม 4 คอลัมน์ตามรายการทบทวนข้างต้น ไม่ใส่คำว่า รอตรวจ เป็นชื่อคอลัมน์
- PK อัตโนมัติครบ 14 ตาราง เริ่มที่ 1 และเพิ่มครั้งละ 1; เลขอาจข้ามได้ ไม่ใช้ ID แทนจำนวนรายการหรือปีงบประมาณ
- FK ที่มีปลายทางในร่าง 3 จุด: Leaves → LeaveTypes, Leaves → FiscalRounds, MobilePermissions → roles
- ยังไม่ได้ทดลองรันใน PostgreSQL/Supabase จึงไม่ถือว่าผ่านการทดสอบการสร้างจริง
- ก่อนนำเข้าต้องจัดทำ mapping รหัส Firebase/ข้อความเดิมไปยัง ID ใหม่ โดยไม่คาดว่าเลขรันใหม่ตรงกับรหัสเดิม

อ้างอิง: [Supabase — Tables and Data](https://supabase.com/docs/guides/database/tables), [PostgreSQL — Identity Columns](https://www.postgresql.org/docs/current/ddl-identity-columns.html), [PostgreSQL — Bit String Types](https://www.postgresql.org/docs/current/datatype-bit.html), [Supabase — Row Level Security](https://supabase.com/docs/guides/database/postgres/row-level-security)
